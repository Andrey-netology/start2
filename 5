<?php
$user = round (0,100)
$x = 1
$y = 1

if ($x > $user)
    echo 'задуманное число Не входит в числовой ряд'
else
    if ($x != $user)
        echo 'задуманное число входит в числовой ряд'
else
                $z = $x;
                $x += $y;
                $y = $z;
for ($z = 1, $z < $y, z$++)
    if (z$ = 1)
break
?>
